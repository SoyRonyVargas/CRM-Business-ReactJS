import { CCard } from "@coreui/react";
import styled from "styled-components";

export const CardWrapper = styled(CCard)`
    border: 0;
    a
    {
        text-decoration: none;
        color: #000;
    }
`